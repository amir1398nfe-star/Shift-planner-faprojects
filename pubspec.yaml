name: shift_planner
description: "Shift & Work Tracker App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  shamsi_date: ^1.0.3
  shared_preferences: ^2.2.2
  fl_chart: ^0.66.2
  screenshot: ^3.0.0
  image_gallery_saver: ^2.0.3
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
