name: shift_planner
description: "A new Flutter project for work shift and leave management."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shamsi_date: ^1.1.1
  screenshot: ^3.0.0
  image_gallery_saver_plus: ^5.1.1
  shared_preferences: ^2.5.5
  fl_chart: ^0.66.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
